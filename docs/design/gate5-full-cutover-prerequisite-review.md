# Gate 5 Full-Cutover-Prerequisite Review

Status: refreshed mandatory human stop. Repository-local and uninstalled.

The first Gate 5 approval exposed two live-installation defects before any real
machine mutation: the Hook adapter expected fields Codex does not send, and the
continuous Worker did not run the retrieval Shadow path. The approved
candidate was therefore not installed. This refreshed review supersedes that
candidate and requires a short renewed Gate 5 approval because its source hash
and operating contract changed.

The authoritative generated evidence is `artifacts/evidence/gate5.json`.
It covers the accepted purge executor, the complete synthetic irrelevant
repair loop, managed integration preservation and divergence behavior, exact
cutover/rollback rehearsal, and the SHA-256 identity of the frozen Shadow
candidate.

The managed exercise copies the current real Codex configuration and Hook
document into an operating-system temporary HOME. All installation, repair,
uninstall, and cutover effects occur there. Before/after digests additionally
prove that the real Vault, Runtime location, Codex files, global Skills, and
LaunchAgent remained unchanged.

Approval of Gate 5 authorizes installation of exactly
`config/gate5-shadow-v1.json` and begins the official seven-consecutive-day
Shadow window. It does not authorize automatic injection, native-memory
replacement, Full Cutover, deletion/import of native data, commit, push, or
publication. Gate 6 remains the mandatory approval for the one Full Cutover.

The refreshed prerequisite additionally proves that official Codex Hook input
is captured with local event identity and time, the Worker maintains the
approved retrieval index and evaluates eligible events asynchronously, its
LaunchAgent receives explicit Luna, embedding, and notifier locations, and the
official window can start only from a completed trusted-Hook probe. Its baseline
records exact candidate/config/Hook/index identities and the earliest Gate 6
review time seven consecutive days later.
